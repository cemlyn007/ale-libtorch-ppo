{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "bazel build --compilation_mode=dbg //src:ale_example",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}