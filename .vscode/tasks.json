{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "bazel build --compilation_mode dbg --spawn_strategy=local --features=oso_prefix_is_pwd --strip=never //src:ale_example",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}