diff --git a/BUILD.bazel b/BUILD.bazel
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -14,6 +14,9 @@ cc_library(
 cc_library(
     name = "yaml-cpp",
     visibility = ["//visibility:public"],
+    # yaml-cpp 0.8.0 uses uint16_t/uint32_t in src/emitterutils.cpp without
+    # including <cstdint>; newer GCC no longer pulls it in transitively.
+    copts = ["-include", "cstdint"],
     includes = ["include"],
     hdrs = glob(["include/**/*.h"]),
     srcs = glob(["src/**/*.cpp", "src/**/*.h"]),
